# Skill symlinks into the tool submodules — generated by link-skills.sh, never
# tracked (a tracked symlink dangles when its tool isn't initialized).
/.claude/skills/

# Generated by `host-lifecycle book` (the canonical publisher); never tracked.
/book.toml
/mdBook/

# The Where room: materialized software bare stores + worktrees are local,
# gitignored; the host commits the .host-software recipe, not the trees.
/software/
